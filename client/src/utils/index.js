//Este archivo nos servirá para crear funciones que vamos a utilizar multiples veces en nuestra página

//Función que calcula los días que quedan teniendo en cuanta el dia actual.
export const daysLeft = (deadline) => {
    const difference = new Date(deadline).getTime() - Date.now();
    const remainingDays = difference / (1000 * 3600 * 24);
  
    return remainingDays.toFixed(0);
  };
  
  //Función que calcula una barra de porcentage dependiendo del dinero aportado
  export const calculateBarPercentage = (goal, raisedAmount) => {
    const percentage = Math.round((raisedAmount * 100) / goal);
  
    return percentage;
  };
  
  //Función que comprueba si la imagen es correcta
  export const checkIfImage = (url, callback) => {
    const img = new Image();
    img.src = url;
  
    if (img.complete) callback(true);
  
    img.onload = () => callback(true);
    img.onerror = () => callback(false);
  };